# Set the general colorscheme to Tomorrow Night.
foreground            #aebac6
background            #1c2129
selection_foreground  #aebac6
selection_background  #213850
url_color             #ec775c
cursor                #aebac6

# Set the font to SF Mono.
font_family      Liga SFMono Nerd Font
bold_font        auto
italic_font      auto
bold_italic_font auto
font_size 12.0
adjust_line_height 120%

# Add padding to the terminal.
window_padding_width 8.0

# Set the cursor to a beam instead of a block.
cursor_shape beam

# Set the extra colors also to GitHub Dark.
# Black
color0   #535d68
color8   #636e7b
# Red
color1   #f47067
color9   #ff9489
# Green
color2   #57ab5a
color10  #6bc46d
# Yellow
color3   #c69027
color11  #dbaa3f
# Blue
color4  #549bf5
color12 #6cb6ff
# Magenta
color5   #b083f0
color13  #dcbdfb
# Cyan
color6   #39c5cf
color14  #56d5dd
# White
color7   #8f9dab
color15  #cdd9e5

# Configure the tab bar.
tab_bar_style slant
tab_bar_background #1c2128
active_tab_foreground   #ec775c
active_tab_background   #22272f
active_tab_font_style   bold-italic
inactive_tab_foreground #aebac6
inactive_tab_background #636e7b
inactive_tab_font_style normal

# Make the scrollback infinite.
scrollback-lines -1

# Run under Xorg to use a native window decoration under Wayland.
#linux_display_server x11

# Add a binding for Kitten search.
# map ctrl+f      launch --location=hsplit --allow-remote-control kitty +kitten search.py @active-kitty-window-id

# Disable the titlebar for Macs.
hide_window_decorations titlebar-only
